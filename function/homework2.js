const add =(a, b) =>{
    const num1 =prompt('unesi prvi broj:');
    const num2 = prompt('unesi drugi broj:');

    const sum = +num1 + +num2;
    prompt('Zbir vasa dva broja je:', +sum);
    return (+num1 + +num2);
}
add();












/*const add =(a,b) =>{
    const broj1 = prompt('Unesi prvi broj:');
    const broj2 = prompt('Unesi drugi broj:');

    const sum = a + b;
    return (a + b);
}
prompt(add(12, 1));*/