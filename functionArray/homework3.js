/*const DATA1 =[1,3, 5, 2, 7, 6, ];

//el > 2 = el 7
//el > 2 && el < 8  =(el * 4 ) /2

const dogAgeHuman =(ages)=>{
   const humanAge = ages.map((age) => {
       if (el > 2 && el < 8 ) {
           return (el * 4) /2;
       }else if(el > 2){
           return el * 7;
       }

    });return  humanAge;

};


console.log(dogAgeHuman(DATA1));


//function Filter
const words = ['telvizor', 'daljinski', 'telefon', 'voda', 'ranac', 'stolica'];
const filterArray = words.filter((el)=>el.length > 4);
console.log(filterArray);

//function map


const nummbers = [2, 5, 3, 1, 10, 8, 7];

const sqnum =nummbers.map((el, index, words)=>{
    console.log('ispisi element:', el);
    console.log('ispi index:', index);
    console.log('ispisi ceo niz:', words);
    //return nummbers;
});

//function map2
const numbers2 =[2, 5, 3, 1, 10, 8, 7];

const sqnum2 = numbers2.map((el, index, numbers2)=>{
    console.log('ispisi samo element:', el);
    console.log('ispisi index:', index);
    console.log('ispisi ceo niz:', numbers2);
});return numbers2;

console.log(sqnum2);








//beleske neke sa casa
const DATA2 =[2,26,38,75,11,29];
const DATA3 =["a", "b", "c", "d", "e"];


//1)NAPRAVOTI PMOCNI NIZ KOJI CE DA SADRZI OVA DVA NIZA SPOJENA    2)IZ POMOCMNOG NIZA NAPRAVITI JOS JEDNOG POMONOG NIZA IZVADITI SVE PARNER BROJEVE 3)U  taj niz umetniuti broj 10 i broj 20 i vrati prvi pomcni i durgi pomocni niz

const Uradi =(array1, array2)=>{
    const spoj = array1.concat(array2);
    const spoj2 =[];

    for (let el of spoj2) {
        if (el% 2 === 0) {
            spoj.push(el);
        }
        
    }return (array1, array2)

}

console.log(Uradi(DATA2, DATA3));

//function filter


const words2 = ['telvizor', 'daljinski', 'telefon', 'voda', 'ranac', 'stolica'];
const filterArray2 = words.filter((el)=>el.length > 4);
console.log(filterArray2);

//function map
const nummbers3 = [2, 5, 3, 1, 10, 8, 7];

const sqnum3 =nummbers.map((el, index(0,3, 5))=>{

});

//

const DATA4 =[1,3, 5, 2, 7, 6, ];

//el > 2 = el 7
//el > 2 && el < 8  =(el * 4 ) /2

const dogAgeHuman4 =(ages)=>{
   const humanAge = ages.map((age) => {
       if (el > 2 && el < 8 ) {
           return (el * 4) /2;
       }else if(el > 2){
           return el * 7;
       }
    });return humanAge;
};
console.log(dogAgeHuman4(DATA4));



const Uradi4 =(array3, array4)=>{
    const spoj = array3.concat(array4);
    const spoj2 =[];

    for (let el of spoj2) {
        if (el% 2 === 0) {
            spoj.push(el);
        }
        
    }return ()

}

console.log(Uradi);

//sa casa zdatak
const DATA3 =[2,26,38,75,11,29];
const DATA4 =["a", "b", "c", "d", "e"];

const aWork = (array5, array6) =>{
    const spoj = array5.concat(array6);
    const spo3 = [];
    for(let el of spoj3){
        if (el % 2 === 0) {
            spoj.push(el);
        }
    }return [array2, array4];
}
console.log(aWork);

*/