/*Напишите функцију која конвертује низ вредности из миља у километре користећи метод мапе. На крају, додајте километре у нову променљиву под називом "тоталДистанцеИнКилометерс" и вратите ову променљиву.
 
константне миље = [15,20,1,60,45,120]*/

const miles = [15,20,1,60,45,120];


const convert =(array) =>{
    const convert = array.map((el)=>el *1.6);
    const back =
    //const totalDistanceInKilometers = array.p
}

console.log(convert(miles))
